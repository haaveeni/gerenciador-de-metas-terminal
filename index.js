// hello world
let mensagem = 2
console.log(mensagem)

// arrays, objetos
// let metas = ["veri", "alo"]

// console.log(metas[1] + ", " + metas[0])

let meta = {
  value: 'ler um livro por mês',
  checked: false,
  // log: (info) => {
  //   console.log(info)
  // }
}

let metas = [
  meta,//ler um livro por mes, meta[0]
  {
    value: "estudar inglês 15 minutos todos os dias",//meta[1]
    checked: false,
  }
]

// console.log(meta.checked);

// function // arrow function
// const criarMeta = () => {} 

// function criarMeta() {}

// meta.value = "Não é mais ler um livro"
// meta.log(meta.value)